//rest operator, ... is a rest operator we can use in function. for printing multiple parameter value

// same ... operator used in array is called a Spread operator. it can be stored all array elements. 

function sum(...num){
    console.log(num);
}

sum(10,20, 30, 40, 50, 60);
