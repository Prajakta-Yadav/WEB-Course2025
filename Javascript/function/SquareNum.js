function square(num){
    return num*num;
}

console.log(square(20));

//function name = argument    condition 
let squarenum = (num) => num*num;

console.log(squarenum(30));
squarenum(40);  //with parameter function need to write in console statement. but without parameter or default function does not need console statement. we can directly call it. 